# 数据与密钥治理

- `.env`、`data/`、`legacy-data/`、构建产物和用户生成文件不得提交。
- `demo-data/` 只能保存人工编写、不可关联真实个人的固定 JSON；不得复制生产数据库、对话、报告或对象存储文件。
- 提交前检查 `git status`，并使用密钥扫描工具检查当前内容和完整历史。
- 当前仓库曾在初始提交中包含旧 H2 文件和生成 PDF。本次已取消后续跟踪；公开仓库前应在团队确认后使用 `git filter-repo` 重写历史、轮换历史中出现过的所有真实凭据，并协调所有协作者重新克隆。

历史重写属于破坏性维护操作，不应在普通功能提交中自动执行。
