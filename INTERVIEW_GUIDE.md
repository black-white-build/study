# HeartPilot 面试准备手册

## 1. 30 秒项目介绍

HeartPilot 是我基于一个公开 Spring AI 教学项目继续开发的关系成长系统。上游主要提供大模型、RAG、工具调用和 ReAct 教学骨架；我重点完成了多用户持久化、JWT 数据隔离、确定性任务状态机、人工确认与重规划、Redis 租约锁、幂等和恢复机制、执行轨迹，以及报告、计划和打卡的产品闭环。项目没有宣称生产级可靠，目前验证的是 H2 环境下的关键路径和可重复构建。

## 2. 两分钟讲解顺序

1. **业务问题**：普通聊天只能给建议，用户很难把建议转化为可确认、可执行、可复盘的行动。
2. **核心取舍**：业务状态由确定性状态机控制；模型只生成内容，ReAct 只核验公开信息，避免模型直接改变业务状态。
3. **主链路**：用户创建任务 → 幂等入库 → 获得任务锁 → 分步检索 → 保存证据 → 用户确认/修改 → 生成最终报告 → 可选 PDF。
4. **异常链路**：工具超时、任务取消、心跳过期、退避重试、Redis 不可用时的能力降级。
5. **边界**：现有测试不包含真实 PGVector/Redis/MinIO 组合和系统化 AI 评测，所以只证明关键实现，不证明生产容量。

## 3. 必须能画出的架构

```text
Vue 3
  ├─ REST/JWT ───────────────┐
  └─ POST-SSE ───────────────┤
                              ▼
Spring Boot Controller → Application Services → JPA/Flyway → PostgreSQL
                              │                    └────────→ PGVector
                              ├─ Redis：限流、任务租约锁
                              ├─ MinIO/Local：文档与 PDF
                              ├─ DashScope：对话、结构化输出、Embedding
                              └─ 白名单 Tools/MCP：网页、地图、路线、图片
```

解释时强调：`AgentTaskService` 是用例编排层，输入、研究、步骤推进和最终报告已经拆成独立服务。

## 4. 高频拷打问题与回答要点

### Q1：为什么不让 ReAct Agent 自己决定完整流程？

模型输出不稳定，业务状态又要求可恢复和可审计。项目采用“确定性状态机 + 受限 Agent”混合架构：状态迁移、确认和重试由代码控制；Agent 只决定是否调用白名单公开信息工具。这样牺牲一部分自主性，换来流程可解释和失败后可恢复。

### Q2：你的项目到底是不是自主 Agent？

不是完全自主 Agent。主任务是固定阶段的工作流，ReAct 是其中可选的公开信息研究组件。准确描述是“带受限 ReAct 能力的可恢复任务编排系统”。

### Q3：状态机解决什么问题？

它把允许的迁移集中在枚举规则中，阻止终态重新运行、跳过确认或重复完成。数据库状态是事实来源，SSE 只是展示通道；浏览器断开不会丢失任务状态。

### Q4：乐观锁和 Redis 锁为什么都要？

Redis 锁降低多个实例同时执行同一任务的概率；JPA `@Version` 防止最终数据库写覆盖。前者处理执行互斥，后者保护持久化并发，它们解决的问题不同。

### Q5：Redis 锁如何避免误删别人的锁？

加锁值使用随机 token；续租和释放通过 Lua 原子比较当前值，只有持有相同 token 的实例才能续租或删除。TTL 避免实例崩溃后永久死锁。

### Q6：Redis 不可用时降级到本地锁还有多实例安全吗？

没有。本地锁只保证单实例内互斥，是可用性优先的明确降级。多实例且 Redis 故障时仍需依赖数据库乐观锁和幂等记录减少重复影响，不能把它描述为严格分布式互斥。

### Q7：幂等是怎么实现的？

创建请求使用“用户 ID + Idempotency-Key”唯一约束；先查询，遇到并发插入冲突后再次按唯一键读取原任务。工具调用使用“任务 ID + 任务版本 + 步骤 + 工具名”作为幂等键，成功记录可复用。

### Q8：这是不是 exactly-once？

不是。分布式系统中外部副作用很难保证 exactly-once。这里实现的是重复请求去重和成功结果复用，更接近 at-least-once 执行下的幂等保护。外部工具本身如果有副作用，还需要对方提供幂等键或补偿机制。

### Q9：服务重启后任务怎么恢复？

运行时持续更新 `heartbeatAt`；启动事件和定时任务扫描长时间未更新的 RUNNING 任务，将其转为 RETRY_WAIT，重置正在执行的步骤，再按退避时间重新进入 WAITING/RUNNING。

### Q10：心跳恢复会不会误判？

会。长时间阻塞、数据库延迟或 JVM 暂停都可能被判为失联。现在使用固定阈值，属于简化实现；更完整的方案应结合租约所有者、可配置阈值、执行阶段和监控告警。

### Q11：`Future.cancel(true)` 能保证 HTTP 请求立刻停止吗？

不能保证。它只向执行线程发送中断。`Thread.sleep` 等可中断操作会响应，但第三方 HTTP 客户端是否及时终止取决于实现。因此还应为 HTTP 客户端设置连接、读取和总超时，不能只依赖线程中断。

### Q12：为什么使用 POST-SSE，不直接 WebSocket？

场景主要是一次请求对应单向增量响应，不需要长期双向通道。POST-SSE 能携带 JSON 请求体，同时保留流式消费体验，实现和运维成本更低。代价是浏览器原生 `EventSource` 只支持 GET，因此前端使用 `fetch` 读取流并自行解析事件块。

### Q13：浏览器断开后模型会停止吗？

对话链路保存活动订阅，可在停止接口或回调中 dispose；任务状态写入数据库，不依赖浏览器连接。但断线、取消和底层模型 SDK 之间不是严格原子关系，需要通过持久化状态和幂等避免错误展示。

### Q14：RAG 的完整链路是什么？

上传校验 → MinIO/本地保存 → Tika 提取文本 → 清洗 → 1200 字符窗口、120 重叠切片 → Embedding → PGVector/HNSW → TopK + 相似度阈值 → 拼接来源进入提示词。未配置模型或向量检索异常时使用关键词降级。

### Q15：关键词降级有什么问题？

当前实现会读取全部知识片段再匹配，数据量大时是 O(N) 扫描，只适合演示规模。可改为数据库全文索引、倒排索引或 Elasticsearch，并对文档状态和租户进行过滤。

### Q16：如何防止不同用户读取彼此数据？

JWT 解析当前用户，Service/Repository 查询使用资源 ID 与用户 ID 组合条件；不存在或不属于当前用户都返回 not found，避免泄露资源是否存在。项目有两个用户交叉读取会话失败的集成测试。

### Q17：JWT 放 localStorage 有什么风险？

实现简单，但发生 XSS 时 token 可能被读取。当前结构化文本用 Vue 文本插值而非 `v-html`，降低模型输出注入风险；更严格方案可以使用 Secure、HttpOnly、SameSite Cookie，并配合 CSRF 防护和短期 access token。

### Q18：为什么使用 Flyway，同时本地又是 Hibernate update？

本地 H2 用 `ddl-auto=update` 降低启动门槛；生产 Profile 使用 Flyway 并将 Hibernate 设为 validate。面试时应说明数据库结构的权威来源在生产环境是 migration，而不是 Hibernate 自动改表。

### Q19：Prometheus 指标是否证明性能好？

不证明。指标只是测量入口。没有固定环境、数据量、并发模型和多次采样，就不能给出 P95、吞吐量或成功率结论。本项目不填写未经压测的数据。

### Q20：你相对原项目最重要的贡献是什么？

不要回答“换了 UI 和名字”。重点回答：多用户数据库模型、数据隔离；确定性可恢复任务编排；幂等、锁、心跳和执行轨迹；人工确认与重规划；产品闭环和部署配置。随后打开 `UPSTREAM_CHANGES.md` 对照说明。

## 5. 基础知识复习清单

### Java 与并发

- 虚拟线程适用场景、阻塞 I/O、线程中断语义。
- `Future#get(timeout)`、`cancel(true)`、异常传播。
- `ConcurrentHashMap`、原子操作、内存可见性。
- 线程池耗尽与嵌套提交问题；本项目使用每任务虚拟线程执行器的取舍。

### Spring

- Bean 生命周期、构造器注入、Profile、配置绑定。
- `@Transactional` 边界、自调用问题、`REQUIRES_NEW`。
- Spring Security Filter 顺序和 SecurityContext。
- SSEEmitter 生命周期与 Reactor `Flux` 订阅取消。

### 数据库

- 乐观锁版本号、唯一约束、事务隔离级别。
- 为什么“先查再插”仍可能竞争，以及唯一约束兜底。
- Flyway migration、索引设计、分页和 N+1 查询。

### Redis

- `SET NX PX`、TTL、Lua 原子性、锁续租。
- Redis 故障和主从切换下分布式锁的限制。
- 固定窗口限流与滑动窗口/令牌桶的区别。

### AI 应用

- ChatClient、流式输出、结构化输出。
- Embedding、余弦距离、TopK、HNSW 的基本含义。
- RAG 召回、重排、引用忠实度和 Prompt Injection。
- Tool Calling 与 MCP 的区别；工具白名单与最小权限。
- 确定性 Workflow 与自主 Agent 的适用边界。

## 6. 演示脚本

1. 注册两个用户，证明数据列表相互隔离。
2. 发起流式咨询，展示停止和重新生成。
3. 创建行动任务，展示执行轨迹和地图证据。
4. 在确认页修改城市/预算/问题，展示任务版本增加和重新规划。
5. 确认任务并生成最终报告/PDF。
6. 关闭外部 Key 或 ReAct，展示明确降级而不是虚构地点。

演示前使用专门的演示账号和假数据，不携带本地 `.env`、真实数据库或历史 PDF。

## 7. 不会 AI 性能评测时的诚实回答

推荐回答：

> 我的主要贡献在 Java 后端工程和 Agent 执行边界，当前没有做系统化模型评测，所以我不会声称 RAG 准确率或生产成功率。现阶段通过固定阈值、来源展示和关键词降级保证行为可观察。如果继续完善，我会先建立包含正常问题、无答案问题、提示注入和城市冲突的固定样例集，再记录 Recall@K、引用忠实度、任务完成率、TTFT 和单次成本。

这比编造性能数字更可信，也能表明你知道下一步应该怎么验证。
